import {Box} from '@mui/material';
export default function () {
    return (
        <Box sx={{
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            height: 'calc(100vh - 200px)'
        }}>
            <h1>Person area</h1>
        </Box>
    )
}